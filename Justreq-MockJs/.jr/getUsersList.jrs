// .jr/getUsersList.jrs
const Mock = require('mockjs');

let data = Mock.mock({
  // 属性 list 的值是一个数组，其中含有 6 到 20 个元素
  'list|6-20': [{
      // 属性 id 是一个自增数，起始值为 80001，每次增 6
      'id|+6': 80001,
      'name': '@name'
  }]
});

end(data);
