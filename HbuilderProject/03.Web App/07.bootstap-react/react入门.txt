复习
1.react特点
虚拟dom,jsx语法（开始和结束只有一个节点）,组件化,组件生命周期,单向数据流
语法:

创建组件

var Comp=React.createClass({
 render:function(){
    return(
      //返回jsx语法作为html内容
    );
 }
});
//1.组件名称首字母大写
//2.createClass({object})//创建组件
//3.实现createClass里面的object中的render创建具体组件内容
//4.在render里面通过return(jsx)形式 返回jsx html内容

2.inner css ,css, js ,inner
1.外部 文件的形式
2.通过object形式声明,object中css属性,遵循原生js写法
3.事件react自定义事件机制 onClick={this.事件名称}
4.多个事件和css,可以将js函数或者css object挂载到组件的原型链上面 在进行获取和调用

3.事件机制
参考react官方文档  onClick,onTouchStart,ouTouchMove,onTouchEnd,前面内容小写,后面的关键字大写

4.组件卸载 和组件渲染
ReactDOM.unmountComponentAtNode(在哪个位置卸载组件);//组件必须要被ReactDOM.render()渲染过
ReactDOM.render(<组件名称/>,组件渲染的位置);



